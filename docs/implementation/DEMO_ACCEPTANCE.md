# 2026-09-24 演示主线验收（去敏摘要）

本文件记录此次实际发生的运行，不把小智、旧 support-agent 发布恢复和 v1 模拟测试拼成一次事故。源码交付不是公网部署，也不是完整产品验收。

## 一页结论

- 新 `/demo` 主线已在本机完成：自由业务输入、应用原生 Langfuse、业务结束前事件、选择证据、真实 EvoMap 多身份消息回读、不同角色调查/反证/复核、建议审核退回及新版本认可、同案 single/swarm。
- 复用旧前端、OfficeScene/Star Office、support-agent 与其模型 adapter、ProductService/ExternalRuntime、ProductRegistry v1 及预算/租约/用量；未再造客服或事故表。
- 原生 **E2 session transport** 已真实验证。Section9 仍控制固定有界任务图与模型调用；没有声称使用原生托管 PDRI、动态成员发现、Hub 经验发布或跨机器自治执行。
- **尚未证实 swarm 质量胜出**。干净对照两边都识别关键政策适用性缺证，single 更快且更省。接力证明是容错过程，不是质量/成本优势统计结论。
- 这是 M2 中“调查建议到人工审核”的可演示闭环；通用动态规划、工具循环、v1 生产执行/对账/恢复、多用户权限和完整对标仍有缺口。

## 自动化与浏览器

| 层次 | 本次证据 | 限制 |
| --- | --- | --- |
| 全套后端 | `python -m pytest -q`：337 passed，2 个依赖弃用 warning | 单元/契约/模拟测试不替代真实服务 |
| 业务应用 | instrumentation 安装时原 support-agent 38 tests passed；独立过程不传模型凭据 | 原应用测试不证明所有自由输入正确 |
| 前端 | `npm run build`：TypeScript + Vite 成功 | build 不是交互验收 |
| 浏览器 | 实际输入退款问题；看到真实业务事件、证据勾选、协作消息；退回第 1 版，生成并认可第 2 版；刷新持久化 | 不是全面无障碍认证 |
| 显示 | 桌面和 375×812 窄屏；页面宽 375、内容宽 375，无横向页面溢出；办公室/角色/证据可读 | 其它设备仍待队友检查 |

新增回归覆盖 native 分块/缺片失败关闭、暂停晚到结果拒绝和接管、业务幂等/并发、证据作用域、版本 hash 审核、两来源 v1 任务图到 typed ProposalVersion。旧全套回归继续通过。

## A. 干净同案对照

本机原报告：`.runtime/demo-validation/20260924-032500.json`，`result=passed`。报告使用 UTC；本页日期使用 Asia/Shanghai。

- 业务任务：`business_a363e55ff17affc3db5d9fbd`
- 真实输入：核查订单 1001，用户自述签收 45 天且使用过，询问客服能否直接判断可退款；要求查订单和政策、只读、不申请退款。
- 当前应用 adapter commit：`5d9e86e8ae2f90122d6bfecb3f1ec38c32047a79`，来自固定上游 `860a29ff80f6e9b866e4cf1098c042e0da425f24` 加已提交适配 patch。
- 应用 trace：`ff7375c8d6d80c06d9d30ba8164a40d6`
- 原生 session：`cmueht9md4pi99c3fuf5id8xg`
- 业务结束前事件、应用 trace 来源核验、双方完成、相同证据 hash、相同预算均通过。

| 对比臂 | Run | 已知模型用量 | 未知用量 | 调查耗时 | 结论 |
| --- | --- | ---: | ---: | ---: | --- |
| single | `run_bdcb629c428641da` | 29,697 | 0 | 16.0s | inconclusive / needs_data |
| swarm | `run_862d7032e0084e39` | 62,585 | 0 | 88.8s | inconclusive / needs_data |

双方模型为 `evomap-gpt-5.6-luna`，每 run token 上限 100000。single 使用一个调查员及汇总步骤，获得完整冻结事实；swarm 使用两个按来源分工的调查步骤、独立反例、后续复核及汇总，共五个任务。每个任务本轮均一次成功。两边都没有任意工具循环；原始材料相同，角色 prompt/输出预算不同。用量是治理调查 provider 报告值，不含前置业务执行；耗时包含对应调度和 native 传输，保留每次尝试累计活动时间。

两边都发现“资格工具仅检查 delivered/无已有退款”与 FAQ 条件存在不一致风险，但均指出尚缺政策适用性和独立订单事实。swarm 提出包含正常边界的测试组合，但一个样本不足以推断质量更好。未进行独立盲评或统计显著性检验。

## B. 原生成员暂停与接力

本机报告 `.runtime/demo-validation/20260924-031116.json`，`result=passed`。

- 任务 `business_079afb95c26b1b0de65d43d5`；session `cmuehbhsn4huccc3grnglyik8`。
- swarm `run_89fd954c20cd4834`：暂停 investigator 身份，晚到结果拒绝；保留消耗后人工继续，coordinator 身份接手；该 investigator 任务两次尝试，后续完成。
- single `run_b52fdcf660f34af4` 也完成同案。
- 已知用量分别 58,660 / 23,152，未知用量均 0。故障仅注入 swarm，**不将此组用于公平延迟/成本排名**。
- 这是操作员主动暂停与角色接管，未模拟真实网络分区、主机被杀或多个独立工作主机故障。

## C. 浏览器审核与版本闭环

- 任务 `business_82e0a2f0158d9522e34f97b4`，网页实际自由输入，勾选两个应用 observation 后调查。
- 操作者退回第 1 版，要求区分用户陈述/独立事实，并确认 FAQ 的适用范围。
- 第 2 版 run `run_c8892da376164f48`，31,734 tokens，生成 `needs_data` 建议，明确只读核验和缺失事实。
- 第 2 版 proposal hash `f2c876e4c612e1bb240e1116e35f47eac57d1c57ad6fa8113d6d9f70f1aac73f`；浏览器实际认可，刷新仍显示“建议已认可”；原版和退回意见保留。
- 建议认可不把 Incident 标成 resolved，不执行退款、发布或恢复验证。这里没有声称已修好被治理应用。

## 保留的失败与修正

开发期间实际发生：目标 DB 相对路径失败；原生大消息 HTTP 413；模型角色结构字段不匹配；一次供应商 timeout 留下 unknown usage；错误 evidence 引用被拒绝；源类型契约拒绝；ProposalVersion 严格 enum 与 budget 字段读取问题。已修正关联实现，失败任务/用量保存在本机历史，没有删除来清绿。

早期对照任务的 elapsed_s 只记录最后恢复段，不能当完整耗时。新代码记录所有分析尝试累计时长，上面 A 为修正后的干净结果。B、C 是不同任务，不能互相拼出一个不存在的最终事故。

## 未验证/尚未实现

- 新物理设备冷安装、长期稳定性、多操作者/多进程、公开部署及生产数据。
- 原生托管规划、Hub/市场生命周期、公共能力发现、跨机器 Agent 执行。
- 语义模型在整个业务执行期间持续自主观察；当前是规则观察 + 完成后模型调查。
- 自由输入广泛正确性、用户任意要求下无副作用、完整隐私识别。当前 demo 使用隔离种子数据，去敏器主要过滤凭据，不是通用 PII 分类器。
- 新 v1 与旧 Approval/Execution/Recovery 的完整迁移；生产修复、执行对账、独立恢复证明。
- swarm 质量优势的多案盲评；不得写成已达到评分准则中的全面涌现能力。

进度按“本页可复跑主线”与“完整产品”分别看：前者上述 A/B/C 有实际证据；后者分母包含完整规划 36 功能/83 连接器/4 补充项，缺少逐项独立验收，不给虚假百分比。
