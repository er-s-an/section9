diff --git a/src/llm.py b/src/llm.py
--- a/src/llm.py
+++ b/src/llm.py
@@ -82,6 +82,7 @@ def get_llm(model: str | None = None, temperature: float = 0.0) -> RotatingChatModel:
             api_key=section9_key,
             base_url=base_url,
             temperature=temperature,
+            max_tokens=512,
             max_retries=0,
             timeout=60,
         ))
diff --git a/src/agents/base.py b/src/agents/base.py
--- a/src/agents/base.py
+++ b/src/agents/base.py
@@ -34,6 +34,6 @@ def run_tool_agent(
     user_message: str,
     history: list | None = None,
-    max_rounds: int = 4,
+    max_rounds: int = 2,
 ) -> dict:
     """Generic tool-calling loop shared by every specialist agent."""
     tools_by_name = {t.name: t for t in agent_tools}
diff --git a/src/orchestrator.py b/src/orchestrator.py
--- a/src/orchestrator.py
+++ b/src/orchestrator.py
@@ -107,5 +107,6 @@ def run_orchestrator(user_message: str, history: list | None = None) -> dict:
     result, escalation = call_with_retry(
         lambda: _GRAPH.invoke({"user_message": user_message, "history": history or [], "trace": []}),
+        max_attempts=1,
         escalate_reason="Agent pipeline failed (e.g. rate limit or API error)",
         escalate_context=user_message,
     )
